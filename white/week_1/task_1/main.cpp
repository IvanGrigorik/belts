#include <iostream>
using namespace std;

/*
 * Two integers passed via standard input, modulo not exceeding 1000.
 * Goal: Print their sum to standard output.
 */

int main() {
    int x, y;
    cin >> x >> y;
    cout << x + y;
    return 0;
}
